/* Implementação melhorada para obter o maximo e o minimo*/
void MaxMin2(TipoVetor A, int *Max, int *Min)
{int i;
*Max = A[0]; *Min = A[0];
for (i = 1; i < N; i++)
 { if (A[i] > *Max) *Max = A[i];
  else if (A[i] < *Min) *Min = A[i];
 }
}
