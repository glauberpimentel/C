/* Implementação direta para obter o maximo e o minimo */
void MaxMin1 (TipoVetor A, int *Max, int *Min)
{ int i;
*Max = A[0]; *Min = A[0];
for (i = 1; i < N; i++)
{ if (A[i] > *Max) *Max = A[i];
  if (A[i] < *Min) *Min = A[i];
}
}
